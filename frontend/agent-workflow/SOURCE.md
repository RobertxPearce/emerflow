# Vendored: @hospital-swarm/agent-workflow

Copied unchanged from https://github.com/RobertxPearce/emerflow (`agent-workflow/ui`, commit f1d9f36),
our teammate's workflow view. Emer Flow feeds it live swarm rounds through `src/hospital/workflowAdapter.js`
instead of the package's own SimPy simulation. To update: copy `agent-workflow/ui/{src,styles.css,package.json}` again.
