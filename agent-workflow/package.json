{
  "name": "agent-workflow",
  "private": true,
  "workspaces": [
    "ui",
    "demo"
  ],
  "scripts": {
    "setup": "python3 -m venv server/.venv && server/.venv/bin/pip install -r server/requirements.txt",
    "server": "cd server && .venv/bin/uvicorn server:app --port 8000",
    "demo": "npm run dev --workspace agent-workflow-demo",
    "check": "cd server && .venv/bin/python check.py && cd .. && npm run typecheck --workspace @hospital-swarm/agent-workflow && npm run lint --workspace agent-workflow-demo && npm run build --workspace agent-workflow-demo",
    "record": "cd server && .venv/bin/python record_fixture.py",
    "css": "npm run build:css --workspace @hospital-swarm/agent-workflow"
  }
}
