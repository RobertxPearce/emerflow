# @hospital-swarm/agent-workflow

React package that shows the Hospital Swarm decision process live. Import from
`@hospital-swarm/agent-workflow` only; `src/index.ts` is the public API.

Documentation, including how it works, the event protocol, integration and adapting to other
data, is in [`../README.md`](../README.md).
