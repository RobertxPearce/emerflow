# @hospital-swarm/capacity-map

React package for the public hospital capacity map: live ER load, closest hospitals, emergency
news and the what-if incident simulator. Import from `@hospital-swarm/capacity-map` only;
`src/index.ts` is the public API.

Documentation, including data sources, integration and the simulator, is in
[`../README.md`](../README.md).
